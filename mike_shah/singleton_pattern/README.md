# Singleton Pattern

The Singleton pattern is a creational design pattern, that means it is concerned with how we create objects.

The Singleton pattern allows/enforces only the creation of one instance of an object.

one of the ways to enfore one instance is to prevent object creation through public constructor.
